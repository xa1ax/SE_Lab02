/* This program is just an example one under VCS. */
#include <iostream>

using namespace std;

int main()
{
    int a, b;
    cout << "Enter two integer number" << endl;
    cin >> a >> b;
    cout << "A + B = " << a + b << '\n'
         << "A - B = " << a - b << '\n'
         << "A * B = " << a * b << '\n'
         << "A / B = " << a / b << '\n';
}
